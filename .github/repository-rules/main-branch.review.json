{
  "status": "REVIEW-ONLY",
  "api_payload": "main-branch.review-only.json",
  "api_submission": "Keep the API payload disabled until the maintainer verifies the check name on a completed pull request and explicitly authorizes repository settings changes.",
  "recovery": {
    "maintainer": "repository administrator",
    "steps": [
      "Record the incident, target commit, and intended recovery in a tracked issue before changing repository rules.",
      "Resolve the failed CI quality aggregate or review condition through a new reviewed commit; never force-push or delete main.",
      "If an administrative ruleset change is unavoidable, make the smallest time-bounded change through the GitHub ruleset UI or API, capture its audit entry, and restore protection before the next merge."
    ]
  }
}
